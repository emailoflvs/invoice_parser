# 📦 InvoiceParser - Starter Package

Вы скачали стартовый пакет проекта **InvoiceParser** - AI-powered системы парсинга документов.

## ✅ Что включено в архив:

- 🏗️ **Полная структура проекта**
- 🐳 **Docker конфигурация** (Dockerfile, docker-compose.yml)  
- 📋 **Зависимости** (requirements.txt, pyproject.toml)
- ⚙️ **Конфигурация** (.env.example)
- 🤖 **Промпты для Gemini** (header.txt, items.txt)
- 📚 **Документация** (README.md, QUICK_START.md)
- 💎 **Core модули** (models.py, errors.py, config.py)
- 📝 **Logging setup**
- 🛠️ **Утилиты** (file_ops, json_compare)
- 📤 **Экспортеры** (JSON, Excel - базовые версии)
- ✅ **Тесты** (структура pytest)

## 🚀 Быстрый старт:

### 1. Распакуйте архив:
```bash
tar -xzf invoiceparser_complete_starter.tar.gz
cd invoice_parser
```

### 2. Настройте конфигурацию:
```bash
cp .env.example .env
nano .env  # Добавьте ваш GEMINI_API_KEY
```

### 3. Установите остальной код:

**⚠️ ВАЖНО:** Для полной функциональности нужно скопировать остальные Python модули из чата Claude:

👉 https://claude.ai/chat/b8356800-9477-49a7-a00b-784be32a6b97

**Список файлов для копирования** см. в `INSTALL_CODE.md`

### 4. Запустите проект:
```bash
docker-compose up --build
```

## 📝 Файлы для копирования из чата:

В чате все файлы помечены как:
```python
# file: путь/к/файлу.py
```

**Приоритет установки:**

1. **Критичные** (для базовой работы):
   - `services/orchestrator.py`
   - `services/gemini_client.py`
   - `adapters/cli_app.py`

2. **Важные** (для расширенной функциональности):
   - `preprocessing/image_preprocessor.py`
   - `preprocessing/pdf_preprocessor.py`
   - `services/test_engine.py`

3. **Дополнительные** (для всех фич):
   - `adapters/telegram_bot.py`
   - `adapters/web_api.py`
   - `adapters/email_poller.py`

## 💡 Альтернативные способы установки:

### Вариант 1: Попросите Claude
Откройте новый чат с Claude и напишите:
```
Создай все недостающие файлы для InvoiceParser из чата
https://claude.ai/chat/b8356800-9477-49a7-a00b-784be32a6b97
```

### Вариант 2: Ручное копирование
1. Откройте чат по ссылке выше
2. Используйте Ctrl+F для поиска `# file:`
3. Копируйте код каждого файла
4. Создавайте файлы в соответствующих директориях

## 📚 Документация:

После распаковки архива прочитайте:
- `QUICK_START.md` - быстрое начало работы
- `README.md` - полная документация проекта
- `INSTALL_CODE.md` - список всех файлов для установки
- `src/CODE_INSTALLATION.md` - детальные инструкции

## 🎯 Структура проекта:

```
invoice_parser/
├── src/invoiceparser/           # Исходный код
│   ├── core/                    # ✅ Модели, конфиг, ошибки
│   ├── infra/                   # ✅ Logging
│   ├── services/                # ⚠️ Копировать из чата
│   ├── preprocessing/           # ⚠️ Копировать из чата  
│   ├── adapters/                # ⚠️ Копировать из чата
│   ├── exporters/               # ✅ JSON, Excel (базовые)
│   ├── utils/                   # ✅ File ops, JSON compare
│   └── app/                     # ⚠️ Entry points (копировать)
├── prompts/                     # ✅ Промпты Gemini
├── tests/                       # ✅ Структура тестов
├── Dockerfile                   # ✅ Docker конфигурация
├── docker-compose.yml           # ✅ Docker Compose
├── requirements.txt             # ✅ Зависимости
├── .env.example                 # ✅ Пример конфигурации
└── README.md                    # ✅ Документация
```

**Легенда:**
- ✅ = Включено в архив, готово к использованию
- ⚠️ = Нужно скопировать из чата

## 🔧 Первый запуск:

После установки всех файлов:

```bash
# CLI режим
docker-compose run --rm app python -m invoiceparser.app.main_cli parse --path /app/invoices/test.pdf

# Web API
docker-compose up app

# Telegram Bot  
docker-compose --profile telegram up

# Email Poller
docker-compose --profile email up

# Тесты
docker-compose run --rm app pytest
```

## ❓ Проблемы?

1. **Ошибка импорта модуля** → Проверьте, что все файлы скопированы
2. **GEMINI_API_KEY не найден** → Проверьте .env файл
3. **Docker ошибки** → Попробуйте `docker-compose down -v && docker-compose up --build`

## 📞 Поддержка:

- Чат с полным кодом: https://claude.ai/chat/b8356800-9477-49a7-a00b-784be32a6b97
- Логи: `logs/` директория
- Конфигурация: `.env` файл

---

**Успешной работы с InvoiceParser! 🎉**

P.S. Не забудьте добавить свой GEMINI_API_KEY в .env файл!
