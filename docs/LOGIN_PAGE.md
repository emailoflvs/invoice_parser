# Страница входа

## ✅ Реализовано

Создана отдельная страница входа `/login.html` с формой логина/пароля.

## 📋 Функциональность

### Страница входа (`/login.html`)

**Поля формы:**
- Имя пользователя
- Пароль (с возможностью показать/скрыть)
- Чекбокс "Запам'ятати мене"
- Ссылка "Забули пароль?"
- Ссылка "Зареєструватися"

**Функции:**
- Валидация полей
- Показ/скрытие пароля
- Сообщения об ошибках/успехе
- Автоматический редирект на главную страницу после успешного входа
- Регистрация нового пользователя (модальное окно)
- Восстановление пароля (заглушка)

### Главная страница (`/`)

**Поведение:**
- Проверка авторизации при загрузке
- Если нет токена → автоматический редирект на `/login.html`
- Если токен есть → показывается страница загрузки/утверждения файлов
- Кнопка выхода (в правом нижнем углу)

## 🔄 Поток работы

1. **Пользователь открывает `/`**
   - Проверяется наличие токена в `localStorage`
   - Если токена нет → редирект на `/login.html`

2. **Пользователь на странице входа**
   - Вводит логин и пароль
   - Нажимает "Увійти"
   - Система проверяет учетные данные через API
   - При успехе → токен сохраняется в `localStorage`
   - Редирект на `/`

3. **Пользователь на главной странице**
   - Может загружать файлы
   - Может утверждать данные
   - Может выйти через кнопку выхода

4. **Выход**
   - Нажатие на кнопку выхода
   - Подтверждение
   - Удаление токена из `localStorage`
   - Редирект на `/login.html`

## 📁 Файлы

- `static/login.html` - страница входа
- `static/login.css` - стили страницы входа
- `static/login.js` - логика страницы входа
- `static/index.html` - главная страница (обновлена)
- `static/script.js` - логика главной страницы (обновлена)

## 🎨 Дизайн

Страница входа имеет:
- Современный дизайн с градиентным фоном
- Центрированную карточку с формой
- Логотип и название системы
- Анимации появления
- Адаптивный дизайн для мобильных устройств

## 🔒 Безопасность

- Пароли не сохраняются в браузере
- Токен хранится в `localStorage`
- Автоматический редирект при отсутствии токена
- Проверка авторизации перед загрузкой файлов

## 📝 API эндпоинты

- `POST /api/auth/login` - вход в систему
- `POST /api/auth/register` - регистрация
- `GET /api/auth/me` - информация о текущем пользователе

## ✅ Результат

Теперь система работает с отдельной страницей входа:
- ✅ Отдельная страница `/login.html`
- ✅ Автоматический редирект при отсутствии авторизации
- ✅ Редирект на главную после успешного входа
- ✅ Кнопка выхода на главной странице
- ✅ Регистрация новых пользователей
- ✅ Заглушка для восстановления пароля

**Система готова к использованию!** 🎉









