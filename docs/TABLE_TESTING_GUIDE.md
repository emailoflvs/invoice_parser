# Руководство по тестированию отображения таблиц

## Быстрая проверка

### 1. Запустите сервер

```bash
# Из корня проекта
cd /home/lvs/Desktop/AI/servers/invoice_parser
python -m uvicorn src.invoiceparser.adapters.web_api:app --reload --host 0.0.0.0 --port 8000
```

### 2. Откройте браузер

```
http://localhost:8000/
```

### 3. Откройте DevTools (F12)

Перейдите на вкладку **Console** - там будут логи порядка колонок.

### 4. Загрузите тестовый документ

Выберите один из примеров:
- `invoices/invoice.jpg` - базовая накладная
- `invoices/lakover.jpg` - накладная с длинным товаром
- `invoices/camozzi.jpg` - накладная с 33+ товарами

### 5. Проверьте в Console

После парсинга документа вы увидите:

```
✓ Using column order from table_data.column_order (explicit array): [line_number, ukt_zed, product_name, quantity, price_excluding_vat, amount_excluding_vat]

=== TABLE COLUMN ORDER SUMMARY ===
Source: table_data.column_order
Total columns: 6
Column order: [line_number, ukt_zed, product_name, quantity, price_excluding_vat, amount_excluding_vat]
Column mapping: {line_number: "№", ukt_zed: "УКТ ЗЕД", product_name: "Товар", ...}
===================================
```

### 6. Проверьте таблицу визуально

✅ **Порядок колонок**: совпадает с `Column order` из логов
✅ **Заголовки**: корректно отображаются (на украинском/русском/английском)
✅ **Числа**: выравниваются по правому краю
✅ **Длинные тексты**: корректно переносятся
✅ **Hover эффект**: строка подсвечивается при наведении
✅ **Sticky header**: заголовок остается видимым при прокрутке вниз

---

## Детальная проверка порядка колонок

### Пример 1: invoice.jpg

**Ожидаемый порядок** (из JSON):
```json
"column_mapping": {
  "line_number": "№",
  "ukt_zed": "УКТ ЗЕД",
  "product_name": "Товар",
  "quantity": "Кількість",
  "price_excluding_vat": "Ціна без ПДВ",
  "amount_excluding_vat": "Сума без ПДВ"
}
```

**Проверка в таблице**:
```
| № | УКТ ЗЕД | Товар | Кількість | Ціна без ПДВ | Сума без ПДВ |
```

### Пример 2: lakover.jpg

**Ожидаемый порядок**:
```json
"column_mapping": {
  "line_number": "№",
  "article": "Артикул",
  "product_name": "Товари (роботи, послуги)",
  "application_mode": "Режим полімерізації/ Тип нанесення",
  "ukt_zed": "Код УКТЗЕД",
  "quantity": "Кількість",
  "price_excluding_vat": "Ціна без ПДВ",
  "amount_excluding_vat": "Сума без ПДВ"
}
```

**Проверка в таблице**:
```
| № | Артикул | Товари (роботи, послуги) | Режим полімерізації/ Тип нанесення | Код УКТЗЕД | Кількість | Ціна без ПДВ | Сума без ПДВ |
```

### Пример 3: camozzi.jpg

**Ожидаемый порядок**:
```json
"column_mapping": {
  "line_number": "N.",
  "product_name": "Номенклатура товарів/послуг",
  "quantity": "Кіл-сть",
  "unit": "Од",
  "price_excluding_vat": "Ціна без ПДВ, ГРН",
  "amount_excluding_vat": "Сума без ПДВ ГРН"
}
```

**Проверка в таблице**:
```
| N. | Номенклатура товарів/послуг | Кіл-сть | Од | Ціна без ПДВ, ГРН | Сума без ПДВ ГРН |
```

---

## Проверка визуальных улучшений

### 1. Hover эффекты

- Наведите курсор на **заголовок колонки** → фон должен стать темнее
- Наведите курсор на **строку таблицы** → вся строка должна подсветиться
- Наведите курсор на **поле ввода** → рамка должна стать темнее

### 2. Focus состояние

- Кликните на **любое поле ввода** → должна появиться синяя рамка с тенью
- Проверьте, что фокус хорошо виден

### 3. Sticky заголовок

- Прокрутите таблицу вниз (если больше 10-15 строк)
- Заголовок должен **оставаться видимым** в верхней части экрана

### 4. Адаптивная ширина

- Изменяйте **ширину окна браузера**
- Таблица должна адаптироваться
- При узком окне должна появиться **горизонтальная прокрутка**
- Справа должна быть видна **тень**, показывающая дополнительный контент

### 5. Zebra striping

- Четные строки должны иметь **слегка отличающийся цвет фона**
- Это улучшает читаемость больших таблиц

### 6. Выравнивание

- **Номера строк (№, N.)**: по центру
- **Цены и суммы**: по правому краю
- **Тексты (названия товаров)**: по левому краю
- **Единицы измерения (шт, кг)**: по центру

---

## Проверка на мобильных устройствах

### Открыть DevTools Mobile View

1. F12 → Toggle device toolbar (Ctrl+Shift+M)
2. Выберите устройство: iPhone 12/13, Samsung Galaxy S20
3. Проверьте:
   - ✅ Таблица прокручивается горизонтально
   - ✅ Поля ввода достаточно большие для тача (min 42px)
   - ✅ Hover эффекты работают на тач-устройствах

---

## Проверка разных типов данных

### Тестовые сценарии:

1. **Короткие номера строк** (1, 2, 3)
   - Должны быть в узкой колонке, по центру

2. **Длинные коды товаров** (8501 51 00 90)
   - Должны отображаться полностью, без обрезки

3. **Очень длинные названия товаров**
   - Должны переноситься на несколько строк
   - Использовать textarea

4. **Цены с запятыми** (4 341,66)
   - Должны быть по правому краю
   - С моноширинными цифрами

5. **Единицы измерения** (шт, кг)
   - Короткие, по центру
   - Не должны занимать много места

---

## Проверка логов в Console

### Ожидаемые логи:

```
✓ Using column order from table_data.column_order (explicit array): [...]

=== TABLE COLUMN ORDER SUMMARY ===
Source: table_data.column_order
Total columns: 6
Column order: [line_number, ukt_zed, product_name, quantity, price_excluding_vat, amount_excluding_vat]
Column mapping: {...}
First item keys: [...]
===================================

Starting column analysis...
Available keys: [...]
Config state: {...}

Column "line_number" (label: "№"): {
  analysis: {...},
  type: "line-number",
  width: "max-content",
  useTextarea: false
}

Column "product_name" (label: "Товар"): {
  analysis: {...},
  type: "text",
  width: "auto",
  useTextarea: true
}

=== COLUMN TYPES FOR RENDERING ===
Key: "line_number" → Type: "line-number" | Width: "max-content" | Textarea: false
Key: "product_name" → Type: "text" | Width: "auto" | Textarea: true
===================================
```

### ⚠️ Предупреждения, которые НЕ должны появляться:

```
❌ CRITICAL: Unmapped columns found in line_items: [...]
❌ Keys in line_items but not in column_mapping (will be HIDDEN): [...]
```

Если вы видите эти предупреждения - **это проблема**, нужно проверить JSON!

---

## Что делать при проблемах

### Проблема: Колонки в неправильном порядке

**Решение:**
1. Проверьте лог `Source:` - откуда берется порядок
2. Если `Source: Object.keys(firstItem)` - проблема в JSON, нет `column_order`
3. Добавьте `column_order` в бэкенде или проверьте `post_processor.py`

### Проблема: Некоторые колонки не отображаются

**Решение:**
1. Проверьте лог `Unmapped columns found`
2. Добавьте отсутствующие колонки в `column_mapping`
3. Проверьте, что ключи в `line_items` совпадают с ключами в `column_mapping`

### Проблема: Таблица слишком широкая

**Решение:**
1. Это нормально для таблиц с большим количеством колонок
2. Должна появиться горизонтальная прокрутка
3. Проверьте, что тень справа показывает дополнительный контент

### Проблема: Sticky header не работает

**Решение:**
1. Проверьте, что в CSS есть `position: sticky; top: 0; z-index: 10;`
2. Проверьте, что у родительского контейнера нет `overflow: hidden`

---

## Заключение

После всех проверок:
- ✅ Порядок колонок строго соответствует JSON
- ✅ Все визуальные улучшения работают
- ✅ Таблица адаптивна и удобна
- ✅ Нет хардкода - все динамически из данных

**Если все проверки пройдены - система работает корректно!** 🎉


