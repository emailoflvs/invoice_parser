# Восстановленные переменные окружения

## 🔧 Что было исправлено

Скрипт `setup_env.py` удалял переменные, которые не были в списке `important_keys`. Теперь все важные переменные добавлены в этот список.

## ✅ Восстановленные переменные

### Telegram Bot
```env
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_IDS=
```

### Email Poller
```env
EMAIL_HOST=
EMAIL_PORT=993
EMAIL_USE_SSL=true
EMAIL_LOGIN=
EMAIL_PASSWORD=
EMAIL_ALLOWED_SENDERS=
EMAIL_POLL_INTERVAL=300
EMAIL_POLL_RETRY_DELAY=60
```

### Google Sheets
```env
SHEETS_SPREADSHEET_ID=
SHEETS_CREDENTIALS_PATH=
SHEETS_HEADER_SHEET=Реквизиты
SHEETS_ITEMS_SHEET=Позиции
EXPORT_ONLINE_EXCEL_ENABLED=
```

## 📝 Что нужно заполнить

Все переменные добавлены в `.env` с пустыми значениями (кроме значений по умолчанию). Нужно заполнить:

1. **TELEGRAM_BOT_TOKEN** - токен от @BotFather
2. **TELEGRAM_ALLOWED_USER_IDS** - ваш Telegram User ID
3. **EMAIL_HOST** - адрес IMAP сервера (например: imap.gmail.com)
4. **EMAIL_PORT** - порт (обычно 993 для SSL)
5. **EMAIL_LOGIN** - ваш email адрес
6. **EMAIL_PASSWORD** - пароль или app password
7. **EMAIL_ALLOWED_SENDERS** - список разрешенных отправителей (через запятую)

## 🔒 Безопасность

**Важно:** Эти переменные содержат чувствительные данные (токены, пароли). Они не должны попадать в git!

## ✅ Исправления в коде

1. **`src/invoiceparser/core/config.py`** - добавлены email поля в Config
2. **`scripts/setup_env.py`** - добавлены все важные переменные в `important_keys`

Теперь при запуске `setup_env.py` эти переменные будут сохраняться!

