# Способы входа в систему

В веб-интерфейсе доступны два способа авторизации:

## 1. 🔑 Вход по токену (Token)

**Как использовать:**
1. Откройте настройки (кнопка с шестеренкой в правом нижнем углу)
2. Выберите вкладку "Токен"
3. Введите ваш JWT токен
4. Нажмите "Зберегти"

**Где взять токен:**
- Получите токен через API: `POST /api/auth/login`
- Или используйте токен, который был сохранен ранее

**Преимущества:**
- Быстрый доступ
- Не нужно вводить пароль каждый раз
- Подходит для автоматизации

## 2. 👤 Вход по логину/паролю (Login/Password)

**Как использовать:**
1. Откройте настройки (кнопка с шестеренкой)
2. Выберите вкладку "Логін / Пароль"
3. Введите имя пользователя и пароль
4. Нажмите "Увійти"

**Что происходит:**
- Система автоматически выполняет вход через API
- Получает JWT токен
- Сохраняет токен для дальнейшего использования
- Вы больше не нужно вводить пароль при каждом использовании

**Регистрация нового пользователя:**
- Нажмите "Немає облікового запису? Зареєструватися"
- Заполните форму регистрации
- После регистрации автоматически выполнится вход

## Переключение между режимами

В модальном окне настроек есть два таба:
- **Токен** - для ввода токена напрямую
- **Логін / Пароль** - для входа по учетным данным

Вы можете переключаться между ними в любое время.

## Безопасность

- Пароли никогда не сохраняются в браузере
- Сохраняется только JWT токен (в localStorage)
- Токен имеет срок действия (по умолчанию 30 дней)
- При истечении токена нужно войти заново

## Примеры использования

### Вход по токену (для разработчиков):
```javascript
// Получить токен через API
const response = await fetch('/api/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username: 'admin', password: 'admin123' })
});
const { access_token } = await response.json();

// Использовать токен в настройках
localStorage.setItem('authToken', access_token);
```

### Вход по логину/паролю (для пользователей):
1. Откройте настройки
2. Выберите "Логін / Пароль"
3. Введите учетные данные
4. Нажмите "Увійти"

## FAQ

**Q: Какой способ лучше использовать?**
- Для обычных пользователей: **Логін / Пароль** (удобнее)
- Для разработчиков/API: **Токен** (быстрее)

**Q: Нужно ли вводить пароль каждый раз?**
- Нет, токен сохраняется в браузере
- Нужно вводить пароль только при первом входе или после истечения токена

**Q: Можно ли использовать оба способа?**
- Да, можно переключаться между ними
- Токен, полученный через логин/пароль, можно использовать в режиме "Токен"

**Q: Что делать, если забыл пароль?**
- Сейчас нет функции восстановления пароля
- Обратитесь к администратору для сброса пароля

**Q: Безопасно ли хранить токен в браузере?**
- Токен хранится в localStorage браузера
- Это стандартная практика для JWT токенов
- Токен имеет срок действия
- Для повышения безопасности используйте HTTPS










