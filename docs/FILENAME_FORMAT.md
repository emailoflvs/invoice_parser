# Формат имен файлов

## 📝 Правила именования файлов

### Формат времени
**Формат:** `ddmmhhmm` (день, месяц, час, минута)

**Примеры:**
- `14121619` = 14 декабря, 16:19
- `05122030` = 5 декабря, 20:30
- `01011500` = 1 января, 15:00

## 📄 Типы файлов

### 1. JSON файлы после парсинга

**Формат:** `{base_name}_{timestamp}.json`

**Примеры:**
- `invoice_14121619.json`
- `document_05122030.json`

**Где создается:**
- После парсинга документа через `Orchestrator.process_document()`
- В директории `output/`

**Особенности:**
- Убрано `_gemini` из имени
- Формат времени: `ddmmhhmm`
- Если есть результаты теста: `{base_name}_{timestamp}_{error_count}errors.json`

### 2. JSON файлы после сохранения (APPROVED)

**Формат:** `{base_name}_{timestamp}_saved.json`

**Примеры:**
- `invoice_14121619_saved.json`
- `document_05122030_saved.json`

**Где создается:**
- После сохранения через `POST /api/save`
- В директории `output/`

**Особенности:**
- `_saved` в конце, перед расширением
- Формат времени: `ddmmhhmm`

### 3. Excel файлы

**Формат:** `{document_number}.xlsx`

**Примеры:**
- `INV-2024-001.xlsx`
- `DOC-2024-123.xlsx`

**Где создается:**
- После сохранения APPROVED данных
- В директории `output/`

## 🔄 Изменения

### Было:
- `invoice_saved_20251214_1623.json` (формат `YYYYMMDD_HHMM`, `_saved` в середине)
- `invoice_gemini_14121619.json` (с `_gemini`)

### Стало:
- `invoice_14121619_saved.json` (формат `ddmmhhmm`, `_saved` в конце)
- `invoice_14121619.json` (без `_gemini`)

## 📊 Примеры

### После парсинга:
```
invoice_14121619.json
```

### После сохранения:
```
invoice_14121619_saved.json
```

### С результатами теста:
```
invoice_14121619_0errors.json
```

### Excel:
```
INV-2024-001.xlsx
```

## ✅ Итог

- ✅ `_saved` в конце файла
- ✅ Формат времени: `ddmmhhmm`
- ✅ Убрано `_gemini` из имен

