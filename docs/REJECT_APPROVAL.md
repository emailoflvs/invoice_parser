# Отмена подтверждения документа

## 🔄 Возможность отмены подтверждения

После подтверждения документа (`status = 'approved'`) теперь можно **отменить подтверждение** и вернуть документ в статус `'parsed'` для повторной проверки.

## 📋 API Endpoint

### POST `/api/reject`

Отменяет подтверждение документа и возвращает его в статус `'parsed'`.

**Требуется авторизация:** Да (Bearer token)

**Request:**
```json
{
  "document_id": 123
}
```

**Response:**
```json
{
  "success": true,
  "message": "Подтверждение документа отменено",
  "document_id": 123,
  "new_status": "parsed"
}
```

**Ошибки:**
- `404` - Документ не найден
- `500` - Ошибка при отмене подтверждения

## 🔄 Что происходит при отмене

1. **Статус документа меняется:**
   - `'approved'` → `'parsed'`
   - Документ снова доступен для редактирования

2. **APPROVED данные остаются в БД:**
   - `document_snapshots` с `snapshot_type='approved'` **не удаляются**
   - `document_fields` с `approved_value_*` **не удаляются**
   - `document_table_sections` с `rows_approved` **не удаляются**

3. **Можно снова подтвердить:**
   - После редактирования можно снова сохранить как APPROVED
   - Создастся новый snapshot с обновленными данными

## ⚠️ Важно

- **APPROVED данные не удаляются** - они остаются в истории
- **RAW данные не изменяются** - они всегда остаются исходными
- **Можно отменять многократно** - нет ограничений
- **Требуется авторизация** - только авторизованные пользователи могут отменять

## 📊 Пример использования

```bash
# Отменить подтверждение документа
curl -X POST http://localhost:8000/api/reject \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"document_id": 123}'
```

## 🔍 Статусы документа

- `'parsed'` - RAW данные сохранены, ожидает подтверждения
- `'in_review'` - Документ на проверке
- `'approved'` - Данные подтверждены (можно отменить)
- `'rejected'` - Документ отклонен
- `'exported'` - Данные экспортированы

## ✅ Итог

Теперь можно **отменить подтверждение** документа и вернуть его для повторной проверки! 🔄

