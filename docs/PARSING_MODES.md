# Режимы парсинга документов: Fast vs Detailed

## 📊 Два режима обработки

Система поддерживает два режима парсинга документов:

1. **"fast" (быстрый)** - ⚡ Быстрая обработка, один запрос
2. **"detailed" (детальный)** - 🔬 Детальная обработка, два запроса

## ⚡ Режим "fast" (быстрый анализ)

### Как работает:

```206:246:src/invoiceparser/services/orchestrator.py
    def _fast_parse_with_gemini(
        self,
        main_image: Path,
        additional_images: Optional[list[Path]] = None
    ) -> Dict[str, Any]:
        """
        БЫСТРЫЙ парсинг документа с помощью Gemini (один запрос)

        Использует быструю модель (GEMINI_MODEL_FAST) и комбинированный промпт (PROMPT_ITEMS_HEADER)
        """
        # 1. Переключается на быструю модель
        self.config.gemini_model = self.config.gemini_model_fast

        # 2. Один запрос с комбинированным промптом
        response = self.gemini_client.parse_with_prompt_file(
            image_path=main_image,
            prompt_file_path=self.config.prompt_items_header,  # Комбинированный промпт
            additional_images=additional_images,
            max_tokens=self.config.image_max_output_tokens_fast  # Меньше токенов
        )
```

### Характеристики:

- **Количество запросов:** 1 запрос к Gemini API
- **Модель:** `GEMINI_MODEL_FAST` (например, `gemini-2.0-flash-exp`)
- **Промпт:** Комбинированный (`PROMPT_ITEMS_HEADER`) - header и items вместе
- **Токены:** `IMAGE_MAX_OUTPUT_TOKENS_FAST` (по умолчанию 4096)
- **Скорость:** ⚡ Быстро (обычно 5-15 секунд)
- **Точность:** Хорошая, но может быть ниже чем detailed
- **Когда использовать:** Для быстрой проверки, простых документов

### Преимущества:
- ✅ Быстрая обработка
- ✅ Меньше запросов к API (экономия квоты)
- ✅ Подходит для простых документов

### Недостатки:
- ⚠️ Может быть менее точным для сложных документов
- ⚠️ Меньше токенов = меньше деталей в ответе

---

## 🔬 Режим "detailed" (детальный анализ)

### Как работает:

```418:491:src/invoiceparser/services/orchestrator.py
    def _parse_with_gemini(
        self,
        main_image: Path,
        additional_images: Optional[list[Path]] = None
    ) -> Dict[str, Any]:
        """
        Парсинг документа с помощью Gemini

        Поддерживает два режима:
        - PARALLEL (по умолчанию): header и items парсятся одновременно
        - SEQUENTIAL: header парсится первым, затем items
        """
        # 1. Функция для парсинга header
        def parse_header():
            response = self.gemini_client.parse_with_prompt_file(
                image_path=main_image,
                prompt_file_path=self.config.prompt_header_path,  # Отдельный промпт для header
                max_tokens=None  # Использует IMAGE_MAX_OUTPUT_TOKENS (обычно 90000)
            )
            return self.gemini_client.parse_json_response(response, "header")

        # 2. Функция для парсинга items
        def parse_items():
            response = self.gemini_client.parse_with_prompt_file(
                image_path=main_image,
                prompt_file_path=self.config.prompt_items_path,  # Отдельный промпт для items
                additional_images=additional_images,
                max_tokens=None  # Использует IMAGE_MAX_OUTPUT_TOKENS (обычно 90000)
            )
            return self.gemini_client.parse_json_response(response, "items")

        # 3. Выполнение (параллельно или последовательно)
        if self.config.parallel_parsing:
            # ПАРАЛЛЕЛЬНОЕ выполнение обоих запросов
            with ThreadPoolExecutor(max_workers=self.config.parallel_workers) as executor:
                header_future = executor.submit(parse_header)
                items_future = executor.submit(parse_items)
                header_data = header_future.result()
                items_data = items_future.result()
        else:
            # ПОСЛЕДОВАТЕЛЬНОЕ выполнение
            header_data = parse_header()
            items_data = parse_items()
```

### Характеристики:

- **Количество запросов:** 2 запроса к Gemini API
- **Модель:** `GEMINI_MODEL` (основная модель, например `gemini-1.5-pro`)
- **Промпты:**
  - Отдельный промпт для header (`PROMPT_HEADER_PATH`)
  - Отдельный промпт для items (`PROMPT_ITEMS_PATH`)
- **Токены:** `IMAGE_MAX_OUTPUT_TOKENS` (обычно 90000) для каждого запроса
- **Скорость:** 🔬 Медленнее (обычно 15-30 секунд)
- **Точность:** Высокая, более детальная
- **Когда использовать:** Для важных документов, сложных структур, максимальной точности

### Преимущества:
- ✅ Высокая точность
- ✅ Больше деталей в ответе
- ✅ Специализированные промпты для header и items
- ✅ Может выполняться параллельно (быстрее)

### Недостатки:
- ⚠️ Медленнее (2 запроса)
- ⚠️ Больше запросов к API (больше квоты)

---

## 📊 Сравнение режимов

| Характеристика | Fast ⚡ | Detailed 🔬 |
|----------------|--------|-------------|
| **Запросов к API** | 1 | 2 |
| **Модель** | `GEMINI_MODEL_FAST` | `GEMINI_MODEL` |
| **Промпт** | Комбинированный | Отдельные для header/items |
| **Токены** | 4096 (по умолчанию) | 90000 (по умолчанию) |
| **Скорость** | ⚡ Быстро (5-15 сек) | 🔬 Медленнее (15-30 сек) |
| **Точность** | Хорошая | Высокая |
| **Параллельность** | Нет (1 запрос) | Да (2 запроса параллельно) |
| **Когда использовать** | Быстрая проверка | Важные документы |

## 🎯 Когда использовать какой режим?

### Используйте "fast" ⚡ когда:
- Нужна быстрая проверка документа
- Документ простой и структурированный
- Нужно сэкономить квоту API
- Время обработки критично

### Используйте "detailed" 🔬 когда:
- Документ важный и нужна максимальная точность
- Документ сложный (много полей, таблиц)
- Нужны все детали и метаданные
- Точность важнее скорости

## ⚙️ Настройка режимов

### В веб-интерфейсе:

```javascript
// Быстрый анализ
parseDocument('fast')

// Детальный анализ
parseDocument('detailed')
```

### В API:

```bash
# Быстрый анализ
POST /api/parse?mode=fast

# Детальный анализ
POST /api/parse?mode=detailed
```

### В Telegram:

Telegram бот использует режим `"detailed"` по умолчанию (как веб-форма).

### В коде:

```python
# Быстрый режим
result = await orchestrator.process_document(
    document_path,
    mode="fast"
)

# Детальный режим
result = await orchestrator.process_document(
    document_path,
    mode="detailed"
)
```

## 🔧 Конфигурация

### Переменные окружения (.env):

```env
# Быстрая модель
GEMINI_MODEL_FAST=gemini-2.0-flash-exp

# Основная модель (для detailed)
GEMINI_MODEL=gemini-1.5-pro

# Комбинированный промпт (для fast)
PROMPT_ITEMS_HEADER=/app/prompts/header+items.txt

# Отдельные промпты (для detailed)
PROMPT_HEADER_PATH=/app/prompts/header.txt
PROMPT_ITEMS_PATH=/app/prompts/items.txt

# Токены
IMAGE_MAX_OUTPUT_TOKENS_FAST=4096
IMAGE_MAX_OUTPUT_TOKENS=90000

# Параллельность (для detailed)
PARALLEL_PARSING=true
PARALLEL_WORKERS=2
```

## 📈 Производительность

### Fast mode:
- **Время:** 5-15 секунд
- **Запросов:** 1
- **Токенов:** ~4096

### Detailed mode (параллельно):
- **Время:** 15-30 секунд (оба запроса одновременно)
- **Запросов:** 2 (параллельно)
- **Токенов:** ~90000 × 2 = 180000

### Detailed mode (последовательно):
- **Время:** 30-60 секунд (запросы друг за другом)
- **Запросов:** 2 (последовательно)
- **Токенов:** ~90000 × 2 = 180000

## ✅ Итог

- **"fast"** = быстро, один запрос, хорошая точность
- **"detailed"** = медленнее, два запроса, высокая точность

**По умолчанию** используется режим `"detailed"` для максимальной точности! 🎯









