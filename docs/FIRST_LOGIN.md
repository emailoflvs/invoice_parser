# Первый вход в систему

## 🔐 Создание первого пользователя

При первом запуске системы нужно создать пользователя для входа.

### Способ 1: Создание через команду (рекомендуется)

Выполните в терминале:

```bash
docker-compose exec app python scripts/create_user.py admin admin123 admin@example.com --superuser
```

Где:
- `admin` - имя пользователя (можете изменить)
- `admin123` - пароль (обязательно измените на безопасный!)
- `admin@example.com` - email (необязательно)
- `--superuser` - сделать пользователя администратором

### Способ 2: Создание обычного пользователя

```bash
docker-compose exec app python scripts/create_user.py username password123 user@example.com
```

### Примеры

**Создать администратора:**
```bash
docker-compose exec app python scripts/create_user.py admin mySecurePassword123 admin@mycompany.com --superuser
```

**Создать обычного пользователя:**
```bash
docker-compose exec app python scripts/create_user.py user1 password123 user1@example.com
```

## 📝 После создания пользователя

1. Откройте http://localhost:8000
2. Нажмите кнопку настроек (⚙️) в правом нижнем углу
3. Выберите вкладку "Логін / Пароль"
4. Введите созданные имя пользователя и пароль
5. Нажмите "Увійти"

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- Сразу после первого входа измените пароль администратора на безопасный
- Не используйте простые пароли типа `admin123` в продакшене
- Храните пароли в безопасном месте

## ❓ Проблемы?

### Пользователь уже существует

Если видите ошибку "Username already registered", значит пользователь уже создан. Попробуйте:
- Вспомнить пароль
- Создать нового пользователя с другим именем
- Или сбросить пароль (требует доступа к БД)

### Не могу создать пользователя

Проверьте:
1. Контейнеры запущены: `docker-compose ps`
2. База данных работает: `docker-compose logs db`
3. Приложение работает: `docker-compose logs app`

## 📋 Проверка существующих пользователей

Посмотреть всех пользователей:

```bash
docker-compose exec db psql -U invoiceparser -d invoiceparser -c "SELECT id, username, email, is_superuser FROM users;"
```

## 🎯 Быстрый старт

```bash
# 1. Создать администратора
docker-compose exec app python scripts/create_user.py admin admin123 admin@example.com --superuser

# 2. Войти в систему
# Откройте http://localhost:8000
# Используйте: admin / admin123
```

**Готово! Теперь вы можете войти в систему.**









