# Блокировка загрузки файлов до авторизации

## ✅ Реализовано

Загрузка документов теперь **заблокирована** до авторизации пользователя.

## 🔒 Механизм блокировки

### 1. Проверка авторизации

Перед любым действием с файлами проверяется наличие токена:

- **Выбор файла** - проверка перед открытием диалога
- **Drag & Drop** - проверка перед обработкой
- **Обработка файла** - проверка перед сохранением

### 2. Визуальная блокировка

Когда пользователь не авторизован:

- Область загрузки становится полупрозрачной (opacity: 0.6)
- Курсор меняется на `not-allowed`
- Показывается предупреждение: "Для завантаження документів потрібна авторизація"
- Input для файлов отключен (`disabled`)

### 3. Сообщения пользователю

При попытке загрузить файл без авторизации:

1. Показывается уведомление: "Будь ласка, спочатку увійдіть в систему"
2. Автоматически открывается модальное окно авторизации
3. После успешного входа загрузка активируется автоматически

## 🎯 Поведение

### До авторизации:

- ❌ Нельзя выбрать файл
- ❌ Нельзя перетащить файл
- ❌ Область загрузки заблокирована
- ✅ Показывается предупреждение
- ✅ Автоматически открывается форма входа

### После авторизации:

- ✅ Можно выбрать файл
- ✅ Можно перетащить файл
- ✅ Область загрузки активна
- ✅ Предупреждение скрыто
- ✅ Все функции доступны

## 🔧 Технические детали

### Функции управления:

```javascript
// Блокировка загрузки
disableFileUpload()

// Разблокировка загрузки
enableFileUpload()
```

### Проверки в коде:

1. **handleFileSelect** - проверка перед выбором файла
2. **handleDragOver** - проверка перед drag over
3. **handleDrop** - проверка перед drop
4. **handleFile** - проверка перед обработкой файла

### Активация после входа:

Загрузка автоматически активируется после:
- Сохранения токена (режим "Токен")
- Успешного входа (режим "Логін / Пароль")
- Успешной регистрации

## 📝 Пример использования

```javascript
// При инициализации
if (!state.authToken) {
    disableFileUpload(); // Блокируем
} else {
    enableFileUpload(); // Разрешаем
}

// После успешной авторизации
state.authToken = token;
enableFileUpload(); // Активируем загрузку
```

## ✅ Результат

Теперь **невозможно** загрузить документ без авторизации. Система:
- Блокирует все способы загрузки
- Показывает понятные сообщения
- Автоматически предлагает войти
- Активирует загрузку после входа

**Безопасность обеспечена!** 🔒

