# Настройка Telegram бота

## 🔑 Необходимые переменные в .env

Для работы Telegram бота нужно добавить в `.env` файл:

```env
# Telegram Bot настройки
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_ALLOWED_USER_IDS=123456789,987654321
```

## 📝 Как получить токен

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 👤 Как получить User ID

1. Откройте Telegram и найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Бот покажет ваш User ID (число, например: `123456789`)

## ⚙️ Настройка .env

Добавьте в конец файла `.env`:

```env
# Telegram Bot настройки
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_ALLOWED_USER_IDS=ваш_user_id
```

**Важно:**
- `TELEGRAM_BOT_TOKEN` - обязательная переменная
- `TELEGRAM_ALLOWED_USER_IDS` - опциональная (если пусто, доступ открыт всем - небезопасно!)

## 🚀 Запуск бота

После настройки переменных:

```bash
# Запуск Telegram бота
docker-compose --profile telegram up -d telegram-bot

# Проверка статуса
docker-compose ps telegram-bot

# Просмотр логов
docker-compose logs -f telegram-bot
```

## ✅ Проверка

Если всё настроено правильно, в логах должно быть:
```
Telegram bot started, polling for updates...
```

Если видите ошибку:
```
InvalidToken: You must pass the token you received from https://t.me/Botfather!
```

Это значит, что `TELEGRAM_BOT_TOKEN` не установлен или пустой.

## 🔧 Исправление

Если переменная пропала из .env:

1. Откройте `.env` файл
2. Добавьте в конец:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен
   TELEGRAM_ALLOWED_USER_IDS=ваш_id
   ```
3. Перезапустите бота:
   ```bash
   docker-compose --profile telegram restart telegram-bot
   ```









