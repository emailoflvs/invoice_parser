# 🚀 Invoice Parser - Быстрый старт

## 🐳 Docker (Рекомендуется) - Запуск за 2 минуты

### 1️⃣ Настройка

```bash
# Скопируйте пример конфигурации
cp env.example .env

# Отредактируйте .env файл
nano .env
```

**Обязательно укажите:**
- `GEMINI_API_KEY` - ваш API ключ от Google Gemini
- `WEB_AUTH_TOKEN` - секретный токен для доступа к API (придумайте сложный)

### 2️⃣ Запуск

```bash
# Запустите контейнер (все зависимости установятся автоматически!)
docker-compose up --build -d
```

### 3️⃣ Использование

1. Откройте браузер: **http://localhost:8000**
2. Нажмите на иконку настроек ⚙️ (правый нижний угол)
3. Введите ваш `WEB_AUTH_TOKEN` из .env файла
4. Загрузите документ и начните работу!

**📖 Подробная документация**: см. [DOCKER_QUICKSTART.md](DOCKER_QUICKSTART.md)

---

## 💻 Локальная установка (без Docker)

### Веб-интерфейс за 3 шага

### 1️⃣ Настройка окружения

```bash
# Скопируйте пример конфигурации
cp env.example .env

# Отредактируйте .env файл
nano .env
```

**Обязательно укажите:**
- `GEMINI_API_KEY` - ваш API ключ от Google Gemini
- `WEB_AUTH_TOKEN` - секретный токен для доступа к API (придумайте сложный)

### 2️⃣ Установка зависимостей

```bash
# Создайте виртуальное окружение (опционально)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 3️⃣ Запуск

```bash
# Простой способ
./run_web.sh

# Или вручную
python -m src.invoiceparser.app.main_web

# Или через uvicorn для разработки
uvicorn src.invoiceparser.adapters.web_api:create_app --reload
```

### 4️⃣ Использование

1. Откройте браузер: **http://localhost:8000**
2. Нажмите на иконку настроек ⚙️ (правый нижний угол)
3. Введите ваш `WEB_AUTH_TOKEN` из .env файла
4. Загрузите документ (PDF, JPG, PNG, TIFF, BMP)
5. Нажмите "Распознать документ"
6. Получите результаты!

---

## 📱 Возможности веб-интерфейса

- ✨ **Drag & Drop** - перетащите файл в область загрузки
- 📊 **Прогресс** - визуальный индикатор обработки
- 🎯 **Результаты** - красивое отображение данных
- 📥 **Экспорт** - скачивание JSON или копирование в буфер
- 📱 **Адаптивный** - работает на мобильных устройствах
- 🔒 **Безопасность** - защита через токен

---

## 🔧 Настройка портов

По умолчанию сервер запускается на порту **8000**.

Чтобы изменить порт, отредактируйте `.env`:

```env
WEB_HOST=0.0.0.0
WEB_PORT=3000  # Ваш порт
```

---

## 🛠️ Устранение проблем

### ❌ Ошибка: "Файл .env не найден"

```bash
cp env.example .env
nano .env  # Заполните необходимые поля
```

### ❌ Ошибка: "401 Unauthorized"

Проверьте, что токен в настройках веб-интерфейса совпадает с `WEB_AUTH_TOKEN` в `.env`

### ❌ Ошибка при парсинге

1. Проверьте `GEMINI_API_KEY` в `.env`
2. Убедитесь, что у вас есть доступ к Gemini API
3. Проверьте качество изображения документа
4. Посмотрите логи: `tail -f logs/invoiceparser_*.log`

### ❌ Порт занят

```bash
# Найдите процесс
lsof -i :8000

# Завершите его
kill -9 <PID>

# Или измените порт в .env
```

---

## 📊 Поддерживаемые форматы

| Формат | Расширение | Макс. размер |
|--------|-----------|--------------|
| PDF | .pdf | 50 MB |
| JPEG | .jpg, .jpeg | 50 MB |
| PNG | .png | 50 MB |
| TIFF | .tiff | 50 MB |
| BMP | .bmp | 50 MB |

---

## 🔐 Безопасность

### Production рекомендации:

1. **HTTPS**: Используйте SSL сертификат
2. **Токен**: Генерируйте сложный токен (32+ символа)
3. **CORS**: Укажите конкретные домены вместо `*`
4. **Rate Limiting**: Добавьте ограничение запросов
5. **Мониторинг**: Настройте логирование и алерты

### Генерация безопасного токена:

```bash
# Linux/Mac
openssl rand -hex 32

# Python
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

---

## 📚 Дополнительные ресурсы

- **Веб-интерфейс**: [static/README.md](static/README.md)
- **API интеграция**: [static/integration-example.html](static/integration-example.html)
- **Основной README**: [README.md](README.md)

---

## 💡 Советы

1. **Качество документов**: Чем лучше качество, тем точнее результат
2. **Формат**: PDF обычно даёт лучшие результаты, чем фото
3. **Язык**: Поддерживаются документы на разных языках
4. **Структура**: Работает лучше со структурированными документами

---

## ⚠️ Обработка ошибок

Приложение включает comprehensive обработку ошибок с понятными сообщениями на английском языке.

### Распространенные ошибки:

| Ошибка | Причина | Решение |
|--------|---------|---------|
| ⚠️ API Quota Exceeded | Превышен лимит запросов API | Подождите или обновите план |
| 🔑 API Key Error | Неверный GEMINI_API_KEY | Проверьте ключ в .env |
| 🔐 Authentication Error | Неверный WEB_AUTH_TOKEN | Введите правильный токен в настройках |
| ⏱️ Request Timeout | Документ слишком большой | Используйте меньший файл |
| 🌐 Network Error | Нет интернета | Проверьте подключение |

**📖 Подробная документация по ошибкам**: см. [ERROR_HANDLING.md](ERROR_HANDLING.md)

---

## 🎉 Готово!

Теперь вы можете парсить инвойсы через красивый веб-интерфейс!

**Нужна помощь?**
- Проверьте логи: `docker-compose logs app`
- См. документацию: [ERROR_HANDLING.md](ERROR_HANDLING.md)
- Docker гайд: [DOCKER_QUICKSTART.md](DOCKER_QUICKSTART.md)

---

**Версия:** 1.0.0
**Последнее обновление:** 2024-12-04

