# ИСПРАВЛЕНИЯ ПРИМЕНЕНЫ - Инструкция по тестированию

## ✅ Что исправлено

### 1. Форматирование чисел
- ✅ Float числа правильно форматируются (4341.66 → "4341.66")
- ✅ Integer числа без .0 (1.0 → "1")
- ✅ Большие числа сохраняют все цифры (8501510090.0 → "8501510090")

### 2. CSS для input
- ✅ `overflow: visible` - текст не обрезается
- ✅ `text-overflow: clip` - показывается полный текст
- ✅ `min-width: max-content` для числовых и кодовых колонок

### 3. Логирование для отладки
- ✅ Проверка наличия колонки "no"
- ✅ Логирование отфильтрованных полей

## ⚠️ ВАЖНО: Старые данные в БД

**Проблема:** Документы, распарсенные ДО применения исправлений, имеют ошибки:
- Числа конвертированы в float и потеряли точность
- Например: `4341.6` вместо `4341.66`

**Решение:** Нужно **распарсить документ ЗАНОВО** после применения исправлений!

## 📋 Инструкция по тестированию

### Шаг 1: Перезапустите сервер

```bash
# Остановите текущий сервер (Ctrl+C)
# Запустите заново:
cd /home/lvs/Desktop/AI/servers/invoice_parser
python -m uvicorn src.invoiceparser.adapters.web_api:app --reload --host 0.0.0.0 --port 8000
```

### Шаг 2: Очистите кэш браузера

**Chrome/Edge:**
1. Откройте DevTools (F12)
2. Правый клик на кнопке обновления
3. Выберите "Очистить кэш и жесткая перезагрузка"
4. Или: Ctrl+Shift+R (hard reload)

**Firefox:**
- Ctrl+Shift+R

### Шаг 3: Распарсите документ ЗАНОВО

1. Загрузите документ через веб-интерфейс
2. Дождитесь парсинга
3. **НЕ используйте старые document_id** (16, 25, 26) - они имеют ошибки!

### Шаг 4: Проверьте результат

**В консоли браузера (F12 → Console) должно быть:**
```
✓ Using column order from table_data.column_order (explicit array): [...]
✅ Column "no" found in allKeys
=== TABLE COLUMN ORDER SUMMARY ===
```

**В таблице должно быть:**
- ✅ Колонка "№" заполнена (1, 2, 3...)
- ✅ УКТ ЗЕД: `8501510090` полностью виден
- ✅ Цены: `4341.66`, `4791.66` с десятичными
- ✅ Суммы: `8683.32`, `9583.32` с десятичными
- ✅ Товар в textarea (если длиннее 30 символов)

## 🧪 Тестовый файл

Создан тестовый файл: `static/test_table_display.html`

Откройте в браузере: `http://localhost:8000/static/test_table_display.html`

Проверяет:
- Форматирование чисел
- Фильтрацию колонок
- Наличие колонки "no"

## 🔍 Отладка

Если проблемы остаются:

1. **Проверьте консоль браузера:**
   - Есть ли ошибки JavaScript?
   - Что показывает лог "Column order summary"?

2. **Проверьте Network tab:**
   - Загружаются ли новые файлы script.js и style.css?
   - Какие заголовки Cache-Control?

3. **Проверьте данные в БД:**
   - Откройте DevTools → Application → Local Storage
   - Или проверьте API ответ: `/api/documents/{id}`

4. **Проверьте что сервер перезапущен:**
   - В логах должно быть: "Application startup complete"
   - Проверьте timestamp файлов: `ls -lht static/script.js static/style.css`

## 📝 Измененные файлы

1. `static/script.js`:
   - Улучшено форматирование чисел (toFixed для float)
   - Добавлено логирование колонки "no"
   - Улучшена детекция line-number (maxLength <= 5)

2. `static/style.css`:
   - `overflow: visible` для input
   - `text-overflow: clip` для input
   - `min-width: max-content` для числовых/кодовых колонок

3. `src/invoiceparser/services/post_processor.py`:
   - Отключена нормализация line_items (оставляем строки)
   - Фильтрация служебных полей из column_order

## ✅ Ожидаемый результат

После всех шагов:
- ✅ Все числа отображаются полностью
- ✅ Колонка "№" заполнена
- ✅ Товар в textarea (если длинный)
- ✅ Порядок колонок правильный
- ✅ Нет обрезанных значений

