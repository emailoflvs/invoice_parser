# 🚀 InvoiceParser - Quick Start

Это стартовый набор проекта InvoiceParser с полной структурой и конфигурацией.

## 📦 Что включено:

✅ Полная структура директорий
✅ Docker и docker-compose конфигурация
✅ requirements.txt со всеми зависимостями
✅ .env.example с настройками
✅ Промпты для Gemini (header.txt, items.txt)
✅ Core модели данных (models.py, errors.py)
✅ Базовая конфигурация (config.py)
✅ README с документацией

## ⚠️ Требуется установить:

Остальные Python модули находятся в чате Claude:
👉 https://claude.ai/chat/b8356800-9477-49a7-a00b-784be32a6b97

Все файлы помечены как:
```
# file: src/invoiceparser/module/filename.py
```

## 🎯 Быстрая установка:

### Вариант 1: Копирование из чата

1. Откройте чат Claude по ссылке выше
2. Найдите все блоки кода с `# file: путь/к/файлу.py`
3. Скопируйте содержимое каждого файла
4. Создайте файлы в соответствующих директориях

### Вариант 2: Через Claude

Откройте новый чат с Claude и напишите:
```
Создай все Python файлы для проекта InvoiceParser из чата
https://claude.ai/chat/b8356800-9477-49a7-a00b-784be32a6b97
```

### Вариант 3: Минимальный запуск

1. Установите только критические файлы:
   - orchestrator.py
   - gemini_client.py
   - cli_app.py
   - json_exporter.py

2. Остальное добавляйте по мере необходимости

## 📋 Список файлов для установки:

См. `INSTALL_CODE.md` в корне проекта

## 🐳 Запуск после установки:

```bash
# 1. Настройте .env
cp .env.example .env
# Отредактируйте .env, добавьте GEMINI_API_KEY

# 2. Запустите через Docker
docker-compose up --build

# 3. Или запустите CLI
docker-compose run --rm app python -m invoiceparser.app.main_cli parse --path /app/invoices/your_doc.pdf
```

## 📞 Поддержка:

Если файлы не работают или нужна помощь:
1. Проверьте, что все файлы скопированы
2. Проверьте .env конфигурацию
3. Посмотрите логи в logs/

## 🎓 Структура проекта:

```
invoiceparser/
├── src/invoiceparser/      # Исходный код
│   ├── core/               # Модели и конфиг
│   ├── services/           # Бизнес-логика
│   ├── preprocessing/      # Обработка изображений/PDF
│   ├── adapters/           # Входные каналы
│   ├── exporters/          # Экспорт результатов
│   └── app/                # Entry points
├── prompts/                # Промпты Gemini
├── tests/                  # Тесты
├── invoices/               # Входные документы
├── output/                 # Результаты
├── logs/                   # Логи
└── examples/               # Эталоны для TEST-режима
```

Успешной работы! 🎉
