# Invoice Parser - Web Interface

Красивый современный веб-интерфейс для AI-powered парсера документов.

## Функциональность

- ✨ **Drag & Drop загрузка файлов** - удобная форма загрузки с поддержкой перетаскивания
- 📊 **Индикатор прогресса** - визуальная обратная связь во время обработки
- 🎯 **Красивое отображение результатов** - структурированное представление данных
- 📥 **Экспорт в JSON** - скачивание и копирование результатов
- 🔒 **Безопасность** - авторизация через токен
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## Поддерживаемые форматы

- PDF
- JPG/JPEG
- PNG
- TIFF
- BMP

## Запуск

### 1. Установка зависимостей

```bash
cd /home/lvs/Desktop/AI/servers/invoice_parser
pip install -r requirements.txt
```

### 2. Настройка токена авторизации

Убедитесь, что в вашем `.env` файле установлен токен:

```env
WEB_AUTH_TOKEN=your_secret_token_here
```

### 3. Запуск сервера

```bash
# Из корневой директории проекта
python -m src.invoiceparser.app.main_web
```

Или:

```bash
# Используя uvicorn напрямую
uvicorn src.invoiceparser.adapters.web_api:create_app --host 0.0.0.0 --port 8000 --reload
```

### 4. Открытие в браузере

Откройте браузер и перейдите по адресу:

```
http://localhost:8000
```

## Использование

1. **Настройка токена**:
   - Нажмите на кнопку настроек (⚙️) в правом нижнем углу
   - Введите ваш токен авторизации
   - Нажмите "Сохранить"

2. **Загрузка документа**:
   - Перетащите файл в область загрузки
   - Или нажмите "выберите файл" для выбора через диалог
   - Поддерживаемый размер: до 50MB

3. **Обработка**:
   - Нажмите кнопку "Распознать документ"
   - Дождитесь завершения обработки

4. **Просмотр результатов**:
   - Информация о документе
   - Таблица товаров и услуг
   - Итоговые суммы
   - RAW JSON для разработчиков

5. **Экспорт**:
   - Скачать JSON файл
   - Скопировать JSON в буфер обмена

## API Endpoints

### POST /parse
Загрузка и парсинг документа

**Headers:**
```
Authorization: Bearer YOUR_TOKEN
```

**Body:**
```
file: multipart/form-data
```

**Response:**
```json
{
  "success": true,
  "data": {
    "invoice_number": "INV-001",
    "invoice_date": "2024-01-15",
    "supplier_name": "Company A",
    "customer_name": "Company B",
    "currency": "USD",
    "items": [...],
    "subtotal": 1000.00,
    "tax_amount": 200.00,
    "total_amount": 1200.00
  },
  "processed_at": "2024-01-15T10:30:00"
}
```

### GET /health
Проверка состояния сервиса

**Response:**
```json
{
  "status": "ok",
  "version": "1.0.0"
}
```

## Технологии

- **Backend**: FastAPI, Python 3.11+
- **Frontend**: Vanilla JavaScript, CSS3, HTML5
- **AI**: Google Gemini 2.5 Pro
- **Icons**: Font Awesome 6.4

## Безопасность

- Токен авторизации хранится в localStorage браузера
- Все запросы к API требуют авторизации
- Временные файлы автоматически удаляются после обработки
- CORS настроен для защиты от несанкционированного доступа

## Настройка для Production

1. **CORS**: В `web_api.py` укажите конкретные домены вместо `"*"`:
   ```python
   allow_origins=["https://yourdomain.com"]
   ```

2. **HTTPS**: Используйте обратный прокси (nginx) с SSL сертификатом

3. **Токен**: Используйте сложный токен и храните его в `.env`

4. **Логирование**: Настройте уровень логирования в production режиме

5. **Лимиты**: Добавьте rate limiting для защиты от DDoS

## Troubleshooting

### Ошибка 401 Unauthorized
- Проверьте токен в настройках
- Убедитесь, что токен совпадает с `WEB_AUTH_TOKEN` в `.env`

### Файл не загружается
- Проверьте размер файла (макс 50MB)
- Убедитесь, что формат файла поддерживается

### Ошибка при парсинге
- Проверьте логи сервера
- Убедитесь, что API ключ Gemini настроен корректно
- Проверьте качество изображения документа

## Лицензия

Proprietary - все права защищены

















